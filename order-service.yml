spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:order-master
    driver-class-name: org.h2..Driver
    username: sa
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
